# Production: CMS runs on a separate Cloudflare Worker until a custom domain
# is set up with same-domain routing. Once domain routing is configured,
# change this to /api to use same-domain requests.
PUBLIC_PAYLOAD_API_URL=https://schnitzel-cms.react-spa.workers.dev/api

